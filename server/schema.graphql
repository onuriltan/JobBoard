type Query {
    jobs :[Job]
}

type Company {
    id: ID!
    name: String
    description: String
}

type Job {
    id: ID! # GraphQL understands id, and ! means required
    title: String
    company: Company
    description: String
}
